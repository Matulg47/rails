<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Identificación</title>

  <!-- Estilos CSS en línea -->
  <style>
    body {
      font-family: 'Arial', sans-serif;
      
      color: #333;
      margin: 0;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      background-color: #dfb6b130; /* Rosa pastel */
      border-radius: 8px;
      box-shadow: 0 0 15px rgba(168, 67, 67, 0.65);
      align-items: center;
      width: 300px;
      text-align: center;
      padding-top: 20px;
      justify-content: center;
      margin-top: 50px;
      padding-bottom:10px;
    }

    h1 {
      color: #C0897E; /* Rosa pastel más oscuro */
    }

    .alert {
      background-color: #f8d7da;
      color: #721c24;
      padding: 10px;
      border: 1px solid #f5c6cb;
      border-radius: 5px;
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-top: 10px;
      color: #333;
      text-align: left;
    }

    input[type="text"],
    input[type="password"] {
      width: 100%;
      padding: 8px;
      margin-top: 5px;
      margin-bottom: 15px;
      box-sizing: border-box;
      border: 1px solid #e44d6d; /* Rosa pastel más oscuro */
      border-radius: 5px;
    }

    input[type="submit"] {
      background-color: #C0897E; /* Rosa pastel más oscuro */
      color: #fff;
      padding: 10px 15px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    input[type="submit"]:hover {
      background-color: #c63452; /* Rosa pastel más oscuro al pasar el ratón */
    }
  </style>
</head>

<body>
 
    <% if @error_message.present? %>
      <div class="alert">
        <%= @error_message %>
      </div>
    <% end %>

    <h1>Identificación</h1>

    <%= form_tag identificando_path, method: :post do %>
      <!-- Campos del formulario -->
      <label for="nombre">Usuario:</label>
      <%= text_field_tag :nombre %>

      <label for="contrasena">Contraseña:</label>
      <%= password_field_tag :contrasena %>

      <!-- Otros campos según tus necesidades -->

      <!-- Botón de envío -->
      <%= submit_tag "Enviar" %>
    <% end %>
  
</body>

</html>
